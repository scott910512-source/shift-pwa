# 홈 화면 위젯 (아이폰)

`widget.js` 는 **Scriptable** 앱에서 도는 iOS 위젯 스크립트입니다.
웹앱과 같은 근무 계산식을 쓰므로 결과가 항상 일치합니다.

```
┌──────────────────────────────────────────┐
│ 8/30 (일)  1일차  휴무 B·D         19:42 │
│ ☀ 주간 C조 · 김민규            ● 근무중  │
│  1공장  임재훈 조은비 황선우 남기훈       │
│  2공장  박광현 전규석 김윤종 이재서       │
│  3공장  하윤슬 표정민 곽동주              │
│ ☾ 야간 A조 · 노용수              20:00~  │
│  1공장  김철수 이영희 박민수              │
│  2공장  하형만 박성현 진영욱              │
│  3공장  한지우 서민경 오도현              │
└──────────────────────────────────────────┘
        ↑ 중간 크기 위젯
```

## 설치 (한 번만)

1. App Store에서 **Scriptable** 설치 (무료)
2. Scriptable 열고 오른쪽 위 **+**
3. 이 저장소의 `widget.js` 전체를 붙여넣기
4. 왼쪽 위 설정 아이콘 → 이름을 **근무표** 로 저장
5. 홈 화면 빈 곳 길게 누르기 → 왼쪽 위 **+** → `Scriptable` 검색
6. 위젯 크기 고르고 **위젯 추가**
7. 추가된 위젯 길게 누르기 → **위젯 편집** → Script: **근무표**
   (Run Script / When Interacting 은 기본값 그대로)

잠금 화면 위젯도 같은 방식으로 추가됩니다.

## 크기별 표시 내용

| 크기 | 내용 |
|---|---|
| 작게 | 날짜 · 주간/야간 조 · 조장 · 휴무 조 (폭이 좁아 조원 이름은 생략) |
| **중간** | 날짜·시간·휴무 + 주간/야간 각각 **조장과 1·2·3공장 근무자 전원** |
| 크게 | 중간과 같은 내용을 더 크게 + 앞으로 4일 근무 |

중간 크기를 권합니다. 조원 이름이 길거나 많으면 글자가 자동으로 줄어들어
잘리지 않습니다.

위젯을 누르면 웹앱이 열립니다.

## 명단이 바뀌면?

**위젯은 다시 설치할 필요가 없습니다.** 위젯이 실행될 때마다
`crew.json` 을 받아오고, 받은 내용을 기기에 캐시합니다.

- 인터넷 O → 최신 명단
- 인터넷 X → 마지막으로 받아둔 명단
- 한 번도 못 받았으면 → 스크립트에 내장된 기본 명단

## 관리자용 — 명단 배포 절차

1. `app.js` 의 `seedCrews()` 수정
2. `app.js` 의 `SEED_VERSION` +1
3. `node tools/sync-crew.js` 실행 → `crew.json` 재생성
4. `sw.js` 의 `CACHE_VERSION` +1
5. `main` 에 푸시

이러면 **웹앱과 위젯이 동시에** 새 명단을 씁니다.
`crew.json` 은 `seedCrews()` 에서 자동 생성되므로 손으로 고치지 마세요.

## 갱신 주기

iOS가 위젯 갱신 시점을 정합니다. 스크립트는 다음 교대 시각(08:00 / 20:00)을
갱신 희망 시점으로 알려주지만, 배터리 상황에 따라 몇 분 늦을 수 있습니다.
날짜·교대 단위로 보는 용도라 문제되지 않습니다.

## 안드로이드

Scriptable은 iOS 전용입니다. 안드로이드는 KWGT + Tasker 조합이 필요한데
훨씬 번거롭습니다. 홈 화면에 **웹앱 아이콘**을 추가해 쓰는 편을 권합니다.
