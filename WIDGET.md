# 홈 화면 위젯 (아이폰)

`widget.js` 는 **Scriptable** 앱에서 도는 iOS 위젯 스크립트입니다.
웹앱과 같은 근무 계산식을 쓰므로 결과가 항상 일치합니다.

```
┌──────────────────────────────────────────────────────────────┐
│ 8/30 (일) │ 1일차  휴무 B·D                          10:34  │
│                                                              │
│ ☀ 주간 C조 · 김민규   ● 근무중 │ ☾ 야간 A조 · 노용수  20:00~ │
│ [1공장] 김혁순 김병로 임재혁   │ [1공장] 곽대력 송상현 문정중 │
│ [2공장] 박광현 전규석 김윤종…  │ [2공장] 하형만 박성현 진영욱 │
│ [3공장] 김인회 김석규 장주원…  │ [3공장] 곽정재 민경찬 구태현…│
└──────────────────────────────────────────────────────────────┘
                    ↑ 중간 크기 위젯 (가로 2단)
```

## 설치 (한 번만)

1. App Store에서 **Scriptable** 설치 (무료)
2. Scriptable 열고 오른쪽 위 **+**
3. **`widget-loader.js`** 전체를 붙여넣기 ← `widget.js` 가 아닙니다
4. 왼쪽 위 설정 아이콘 → 이름을 **근무표** 로 저장
5. 홈 화면 빈 곳 길게 누르기 → 왼쪽 위 **+** → `Scriptable` 검색
6. 위젯 크기 고르고 **위젯 추가**
7. 추가된 위젯 길게 누르기 → **위젯 편집** → Script: **근무표**

잠금 화면 위젯도 같은 방식입니다. 위젯은 여러 개 놓을 수 있습니다
(작게 하나 + 중간 하나 처럼 크기별로 두어도 됩니다).

### 왜 로더를 붙여넣나

`widget-loader.js` 는 실제 위젯 코드(`widget.js`)를 인터넷에서 받아 실행하는
20줄짜리 껍데기입니다. 덕분에 **레이아웃이든 명단이든 바뀌어도 다시 붙여넣을
필요가 없습니다.**

- 받기 성공 → 실행해 보고, 잘 돌면 그 코드를 기기에 저장
- 받기 실패(오프라인) → 마지막으로 잘 돌던 코드 사용
- 새로 받은 코드가 오류를 내면 → 이전 버전으로 되돌림 (배포 사고 방어)

## 크기별 표시 내용

| 크기 | 내용 |
|---|---|
| 작게 | 날짜 · 주간/야간 조 · 조장 · 휴무 (폭이 좁아 조원 이름은 생략) |
| **중간** | **가로 2단** — 왼쪽 주간 / 오른쪽 야간, 각 단에 조장과 1·2·3공장 전원 |
| 크게 | 중간과 같은 2단을 더 크게 + 앞으로 4일 근무 |
| 아주 크게 | 아이패드용. 크게와 같은 구성 |

중간 크기를 권합니다. 두 단은 항상 같은 폭으로 나뉘고, 이름이 길거나
많으면 글자가 자동으로 줄어들어 잘리지 않습니다.

위젯을 누르면 웹앱이 열립니다.

## 뭐가 자동으로 갱신되나

| 바뀌는 것 | 자동? |
|---|---|
| 명단 (조장·조원) | ✅ 위젯 실행 때마다 `crew.json` 을 받아옴 |
| 위젯 레이아웃·기능 | ✅ 로더가 `widget.js` 를 받아옴 |
| 날짜·교대 넘어감 | ✅ iOS가 위젯을 다시 그릴 때 반영 |
| 근무 패턴 계산 | 스크립트 안에서 계산 — 인터넷 불필요 |

즉 **설치 후에는 손댈 일이 없습니다.**

## 관리자용 — 명단 배포 절차

1. `app.js` 의 `seedCrews()` 수정
2. `app.js` 의 `SEED_VERSION` +1
3. `node tools/sync-crew.js` 실행 → `crew.json` 재생성
4. `sw.js` 의 `CACHE_VERSION` +1
5. `main` 에 푸시

이러면 **웹앱과 위젯이 동시에** 새 명단을 씁니다.
`crew.json` 은 `seedCrews()` 에서 자동 생성되므로 손으로 고치지 마세요.

## 갱신 주기

iOS가 위젯을 언제 다시 그릴지 정합니다. 앱처럼 계속 도는 게 아닙니다.

- 스크립트는 **15분 뒤**(또는 그보다 이른 교대 시각)에 갱신해 달라고 요청합니다
- iOS가 이를 참고만 하므로 실제로는 **15분~1시간** 간격이 됩니다
- 배터리 부족·저전력 모드에서는 더 뜸해집니다

위젯 오른쪽 위 시각이 **마지막으로 갱신된 시각**입니다. 그 시간이 한참
전이면 화면 내용도 그만큼 오래된 것입니다.

**지금 당장 최신으로 보려면** Scriptable 앱을 열어 스크립트를 한 번 실행하세요.
정확한 시각이 필요할 때는 위젯을 눌러 웹앱을 여는 편이 확실합니다.

근무조 계산은 위젯 안에서 하므로 인터넷이 없어도 항상 정확합니다.
네트워크가 필요한 것은 이름(명단)뿐입니다.

## 안드로이드

Scriptable은 iOS 전용입니다. 안드로이드는 크롬에서 **앱 설치**로 웹앱을
홈 화면에 추가해 쓰는 것을 권합니다. 위젯이 꼭 필요하면 Tasker + KWGT
조합이 필요한데 유료이고 설정이 번거롭습니다.
